# Fonts for Prometheus C2

Place your font files in this directory, for example:

- `JetBrainsMono-Regular.ttf`
- `JetBrainsMono-Bold.ttf`
- `Inter-Regular.ttf` (or another clean sans font)

These fonts can then be wired into `TerminalTheme.tres` or project settings
via the Godot editor.
