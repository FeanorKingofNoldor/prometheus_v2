"""Prometheus v2 – Opportunity and lambda utilities.

This package contains helpers for working with opportunity-density
(lambda) metrics and forecasts, including adapters that bridge
experiment outputs into the core engines (e.g. UniverseEngine).
"""
